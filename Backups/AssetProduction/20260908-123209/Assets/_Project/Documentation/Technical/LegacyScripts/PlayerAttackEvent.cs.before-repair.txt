namespace SoulHunter.Core.Events
{
    /// <summary>
    /// Learning Comment:
    /// Zero-allocation attack event. System sirf trigger sune ga, heap memory allocate nahi karega.
    /// </summary>
    public struct PlayerAttackEvent : IGameEvent
    {
        // Add specific attack details here in the future if needed (e.g. combo counter)
    }
}

