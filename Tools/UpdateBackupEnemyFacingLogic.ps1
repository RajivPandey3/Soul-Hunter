$ErrorActionPreference='Stop';$projectRoot=(Resolve-Path (Join-Path $PSScriptRoot '..')).Path;$backupRoot='D:\Unity Projects\Soul-Hunter_Backup_20260909_Level01Final';$relative='Assets/_Project/AI/Enemies/EnemyController.cs';$source=Join-Path $projectRoot $relative;$target=Join-Path $backupRoot $relative;Copy-Item -LiteralPath $source -Destination $target -Force;$hash=(Get-FileHash -LiteralPath $source -Algorithm SHA256).Hash;if((Get-FileHash -LiteralPath $target -Algorithm SHA256).Hash-ne$hash){throw 'Backup mismatch'};$manifest=Import-Csv (Join-Path $backupRoot 'manifest.csv');$row=$manifest|Where-Object {$_.Path.Replace('\','/') -eq $relative};if(!$row){throw 'Manifest entry missing'};$row.SHA256=$hash;$row.Bytes=(Get-Item -LiteralPath $source).Length;$manifest|Export-Csv -LiteralPath (Join-Path $backupRoot 'manifest.csv') -NoTypeInformation;[pscustomobject]@{Updated=$relative;Backup=$backupRoot}|ConvertTo-Json
