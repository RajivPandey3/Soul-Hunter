$ErrorActionPreference='Stop';$projectRoot=(Resolve-Path (Join-Path $PSScriptRoot '..')).Path;$backupRoot='D:\Unity Projects\Soul-Hunter_Backup_20260909_Level01Final';$manifestPath=Join-Path $backupRoot 'manifest.csv';$manifest=Import-Csv $manifestPath
foreach($relative in @('Assets/Prefabs/Weapons/Enemy_Entity.prefab','Assets/Prefabs/Bosses/SH10_L01_BossGameplay.prefab')){$source=Join-Path $projectRoot $relative;$target=Join-Path $backupRoot $relative;Copy-Item -LiteralPath $source -Destination $target -Force;$hash=(Get-FileHash -LiteralPath $source -Algorithm SHA256).Hash;if((Get-FileHash -LiteralPath $target -Algorithm SHA256).Hash-ne$hash){throw('Mismatch: '+$relative)};$row=$manifest|Where-Object {$_.Path.Replace('\','/') -eq $relative};if(!$row){throw('Manifest entry missing: '+$relative)};$row.SHA256=$hash;$row.Bytes=(Get-Item -LiteralPath $source).Length};$manifest|Export-Csv -LiteralPath $manifestPath -NoTypeInformation;[pscustomobject]@{Updated=$true;Files=2;Backup=$backupRoot}|ConvertTo-Json
